export default function Home() {
  return (
    <main style={{ maxWidth: 640, margin: "4rem auto", fontFamily: "system-ui" }}>
      <h1>SignalPlan</h1>
      <p>
        Protected audit workspace. Sign-in arrives with the core auth module;
        unauthenticated API requests are already rejected.
      </p>
    </main>
  );
}
