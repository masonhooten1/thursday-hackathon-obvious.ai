{
  "name": "linkedin-to-email",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome MV3 extension: LinkedIn profile URL in, company email out via your own enrichment API key.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "check:manifest": "node scripts/check-manifest.js"
  },
  "devDependencies": {
    "vitest": "^4.1.11"
  }
}
